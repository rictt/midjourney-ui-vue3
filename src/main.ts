import { createApp } from 'vue'
import './styles'
import App from './App.vue'
// import { loadingDirective } from './components/Loading'

createApp(App)
  // .directive('uloading', loadingDirective)
  .mount('#app')
